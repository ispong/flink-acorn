插件部署

```yaml
server:
  port: 30155 # 插件的端口号

acorn:
  plugin:
    log-dir: ~/flink-acorn/log # 插件产生的日志路径
    tmp-dir: ~/flink-acorn/tmp # 插件自动生成的项目临时路径
    server-key: acorn-key # 客户端需要使用到的密钥
    flink-port: 8081 # flink安装服务的端口号
    storage-tmp: false # 是否自动删除项目临时路径的文件
```
